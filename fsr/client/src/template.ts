/**
 * The runtime behind `@snapfire/fsr-authoring/template` for a page the
 * browser mounts: the dialect's placements are React's, re-exported and nothing else.
 */
export { Island, island, Link, Slot } from "./react.js";
export type { IslandProps, LinkProps, SlotProps } from "./react.js";
