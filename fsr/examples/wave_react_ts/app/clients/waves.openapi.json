{
  "openapi": "3.0.3",
  "info": { "title": "Waves", "version": "1.0.0" },
  "paths": {
    "/waves": {
      "get": {
        "operationId": "listWaves",
        "parameters": [
          { "name": "view", "in": "query", "schema": { "type": "string" } },
          { "name": "who", "in": "query", "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "the inbox, under one view",
            "content": { "application/json": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/Summary" } } } }
          }
        }
      }
    },
    "/people": {
      "get": {
        "operationId": "listPeople",
        "responses": {
          "200": {
            "description": "everyone on any wave, and whether they are here now",
            "content": { "application/json": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/Person" } } } }
          }
        }
      }
    },
    "/waves/{id}": {
      "get": {
        "operationId": "getWave",
        "parameters": [{ "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }],
        "responses": {
          "200": {
            "description": "one wave and every blip in it",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Wave" } } }
          }
        }
      }
    },
    "/waves/{id}/blips/{blip}": {
      "patch": {
        "operationId": "editBlip",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "blip", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Rewritten" } } }
        },
        "responses": {
          "200": {
            "description": "the blip as it now stands",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Blip" } } }
          }
        }
      }
    },
    "/waves/{id}/blips": {
      "post": {
        "operationId": "addBlip",
        "parameters": [{ "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Written" } } }
        },
        "responses": {
          "200": {
            "description": "the blip as it was kept",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Blip" } } }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Summary": {
        "type": "object",
        "required": ["id", "title", "participants", "blips", "last"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "participants": { "type": "array", "items": { "type": "string" } },
          "blips": { "type": "number", "format": "double" },
          "last": { "type": "string" }
        }
      },
      "Person": {
        "type": "object",
        "required": ["name", "here", "waves"],
        "properties": {
          "name": { "type": "string" },
          "here": { "type": "boolean" },
          "waves": { "type": "number", "format": "double" }
        }
      },
      "Wave": {
        "type": "object",
        "required": ["id", "title", "participants", "blips"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "participants": { "type": "array", "items": { "type": "string" } },
          "blips": { "type": "array", "items": { "$ref": "#/components/schemas/Blip" } }
        }
      },
      "Blip": {
        "type": "object",
        "required": ["id", "parent", "who", "body", "at", "edited", "editors", "depth"],
        "properties": {
          "id": { "type": "string" },
          "parent": { "type": "string" },
          "who": { "type": "string" },
          "body": { "type": "string" },
          "at": { "type": "string" },
          "edited": { "type": "string" },
          "editors": { "type": "array", "items": { "type": "string" } },
          "depth": { "type": "number", "format": "double" }
        }
      },
      "Rewritten": {
        "type": "object",
        "required": ["who", "body"],
        "properties": {
          "who": { "type": "string" },
          "body": { "type": "string" }
        }
      },
      "Written": {
        "type": "object",
        "required": ["who", "parent", "body"],
        "properties": {
          "who": { "type": "string" },
          "parent": { "type": "string" },
          "body": { "type": "string" }
        }
      }
    }
  }
}
