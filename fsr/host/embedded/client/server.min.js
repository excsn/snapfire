import{patchIsland}from"./boot.min.js";import{refresh}from"./navigator.min.js";import{decodeValue,encodeValue}from"./values.min.js";const STATE_PROP="$s";const islands=new WeakMap;export function isServerIsland(el){return islands.has(el);}export function mountServer(el,module,encoded){const carried=encoded??{};const{[STATE_PROP]:state,...props}=carried;const island={module,props,state:state??{},pending:false,listening:new Set};islands.set(el,island);if(!el.firstElementChild){void step(el,island,null,null);return;}listen(el,island);}export async function patchServer(el,props,encoded){const island=islands.get(el);if(!island)return false;const carried=encoded??encodeValue(props);const{[STATE_PROP]:state,...own}=carried;island.props=own;if(state!==undefined)island.state=state;await step(el,island,null,null);return true;}function eventsBound(el){const types=new Set;for(const bound of Array.from(el.querySelectorAll("[data-sf-on]"))){for(const pair of(bound.getAttribute("data-sf-on")??"").split(" ")){const type=pair.split(":")[0];if(type)types.add(type);}}return types;}function listen(el,island){for(const type of eventsBound(el)){if(island.listening.has(type))continue;island.listening.add(type);el.addEventListener(type,event=>void fire(el,island,type,event));}}function handlerFor(el,target,type){if(!(target instanceof Element))return null;const bound=target.closest("[data-sf-on]");if(!bound||!el.contains(bound))return null;for(const pair of(bound.getAttribute("data-sf-on")??"").split(" ")){const[event,handler]=pair.split(":");if(event===type&&handler!==undefined)return handler;}return null;}async function fire(el,island,type,event){const handler=handlerFor(el,event.target,type);if(handler===null)return;if(type==="submit")event.preventDefault();if(island.pending)return;const target=event.target;const detail={target:{value:target?.value??null,checked:target?.checked??null,name:target?.name??null},key:event.key??null};await step(el,island,handler,detail);}async function step(el,island,handler,event){island.pending=true;el.setAttribute("data-sf-pending","");try{const headers={"content-type":"application/json"};if(typeof window!=="undefined")headers["x-sf-from"]=`${window.location.pathname}${window.location.search}`;const named=handler!==null&&!/^\d+$/.test(handler);const body=JSON.stringify({props:island.props,state:island.state,handler:handler===null||named?handler:Number(handler),event:encodeValue(event)});const res=await fetch(`/_sf/island/${encodeURIComponent(island.module)}`,{method:"POST",headers,body});const text=await res.text();if(!res.ok){console.warn(`sf: island ${island.module} step failed with ${res.status}: ${text}`);return;}const answer=JSON.parse(text);island.state=answer.state;morph(el,answer.html);listen(el,island);if(answer.revalidate)await refresh();}finally{island.pending=false;el.removeAttribute("data-sf-pending");}}export function morph(el,html){const template=document.createElement("template");template.innerHTML=html;morphNodes(el,Array.from(el.childNodes),Array.from(template.content.childNodes),null,{nested:morphNested});}function keyOf(node){if(!(node instanceof Element))return null;const key=node.getAttribute("data-sf-key");if(key!==null)return key;const region=node.hasAttribute("data-sf-island")?node.getAttribute("data-sf-region"):null;return region===null?null:`region:${region}`;}function alike(a,b){if(a.nodeType!==b.nodeType)return false;if(a instanceof Element&&b instanceof Element&&a.tagName!==b.tagName)return false;return keyOf(a)===keyOf(b);}export function morphNodes(parent,old,fresh,end,hooks){let i=0;for(const next of fresh){while(i<old.length&&old[i].parentNode!==parent)old.splice(i,1);const current=old[i];if(current&&alike(current,next)){morphNode(current,next,hooks);i+=1;continue;}const key=keyOf(next);const moved=key===null?null:old.slice(i).find(candidate=>candidate.parentNode===parent&&keyOf(candidate)===key)??hooks.adopt?.(key)??null;if(moved){parent.insertBefore(moved,current??end);const at=old.indexOf(moved);if(at!==-1)old.splice(at,1);old.splice(i,0,moved);morphNode(moved,next,hooks);}else{const imported=document.importNode(next,true);parent.insertBefore(imported,current??end);old.splice(i,0,imported);}i+=1;}for(const stale of old.slice(i)){if(stale.parentNode!==parent)continue;hooks.drop?.(stale);parent.removeChild(stale);}}export function morphElement(current,next,hooks){morphAttributes(current,next);morphNodes(current,Array.from(current.childNodes),Array.from(next.childNodes),null,hooks);}function morphNode(current,next,hooks){if(current.nodeType===Node.TEXT_NODE||current.nodeType===Node.COMMENT_NODE){if(current.nodeValue!==next.nodeValue)current.nodeValue=next.nodeValue;return;}if(!(current instanceof Element)||!(next instanceof Element))return;if(isPropsScript(current))return;if(current.tagName==="SF-I"){hooks.nested(current,next);return;}morphElement(current,next,hooks);}function isPropsScript(el){return el.tagName==="SCRIPT"&&el.hasAttribute("data-sf-props");}function morphNested(current,next){const held=current.nextSibling;const wanted=next.nextSibling;if(!(held instanceof Element)||!(wanted instanceof Element)||!isPropsScript(held)||!isPropsScript(wanted))return;const text=wanted.textContent??"";if(held.textContent===text)return;held.textContent=text;const raw=JSON.parse(text);const island=islands.get(current);if(island){const{[STATE_PROP]:state,...props}=raw;island.props=props;if(state!==undefined)island.state=state;void step(current,island,null,null);return;}void patchIsland(current,decodeValue(raw));}function morphAttributes(current,next){for(const attr of Array.from(current.attributes)){if(!next.hasAttribute(attr.name))current.removeAttribute(attr.name);}for(const attr of Array.from(next.attributes)){if(current.getAttribute(attr.name)!==attr.value)current.setAttribute(attr.name,attr.value);}const focused=typeof document!=="undefined"&&document.activeElement===current;if(!focused&&"value"in current&&"value"in next){const control=current;const wanted=next;if(control.value!==wanted.value)control.value=wanted.value;if("checked"in wanted&&control.checked!==wanted.checked)control.checked=wanted.checked;}}//# sourceMappingURL=server.min.js.map
